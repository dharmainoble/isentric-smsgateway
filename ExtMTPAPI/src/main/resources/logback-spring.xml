<?xml version="1.0" encoding="UTF-8"?>
<configuration>

    <!-- Define log directory - uses catalina.base for Tomcat, java.io.tmpdir for standalone -->
    <property name="LOG_DIR" value="${catalina.base:-${java.io.tmpdir}}/logs/extmtpush" />

    <!-- Console Appender -->
    <appender name="CONSOLE" class="ch.qos.logback.core.ConsoleAppender">
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
    </appender>

    <!-- File Appender -->
    <appender name="FILE" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/extmtpush.log</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} [%thread] %-5level %logger{36} - %msg%n</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/extmtpush.%d{yyyy-MM-dd}.%i.log</fileNamePattern>
            <timeBasedFileNamingAndTriggeringPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedFNATP">
                <maxFileSize>10MB</maxFileSize>
            </timeBasedFileNamingAndTriggeringPolicy>
            <maxHistory>30</maxHistory>
        </rollingPolicy>
    </appender>

    <!-- SMS Traffic Log -->
    <appender name="SMS_TRAFFIC" class="ch.qos.logback.core.rolling.RollingFileAppender">
        <file>${LOG_DIR}/sms-traffic.log</file>
        <encoder>
            <pattern>%d{yyyy-MM-dd HH:mm:ss.SSS} - %msg%n</pattern>
        </encoder>
        <rollingPolicy class="ch.qos.logback.core.rolling.TimeBasedRollingPolicy">
            <fileNamePattern>${LOG_DIR}/sms-traffic.%d{yyyy-MM-dd}.log</fileNamePattern>
            <maxHistory>90</maxHistory>
        </rollingPolicy>
    </appender>

    <!-- Logger for SMS traffic -->
    <logger name="com.isentric.smsserver.controller.SmsController" level="INFO" additivity="false">
        <appender-ref ref="SMS_TRAFFIC" />
        <appender-ref ref="CONSOLE" />
    </logger>

    <!-- Logger for application -->
    <logger name="com.isentric" level="DEBUG">
        <appender-ref ref="FILE" />
    </logger>

    <!-- Logger for Spring Framework -->
    <logger name="org.springframework" level="INFO" />
    <logger name="org.springframework.web" level="DEBUG" />
    <logger name="org.springframework.jms" level="DEBUG" />

    <!-- Logger for Hibernate -->
    <logger name="org.hibernate" level="INFO" />
    <logger name="org.hibernate.SQL" level="DEBUG" />
    <logger name="org.hibernate.type.descriptor.sql.BasicBinder" level="TRACE" />

    <!-- Root Logger -->
    <root level="INFO">
        <appender-ref ref="CONSOLE" />
        <appender-ref ref="FILE" />
    </root>

</configuration>

